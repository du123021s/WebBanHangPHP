@extends('layouts.fashion')


@section('content')
        <h1>Hello !</h1>
@endsection
    {{--  <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        Customer
                    </div>
                    <div class="card-body">
                        <h5>Do you already have an account?</h5>

                        <a href="{{ route('auth.login') }}" class="btn btn-secondary">Login</a>
                        <a href="{{ route('auth.register') }}" class="btn btn-primary">Register</a>
                        <a href="{{ route('sophia.home') }}" class="btn btn-secondary">Cancel</a>
                    </div>

                </div>
            </div>
        </div>
    </div>  --}}


